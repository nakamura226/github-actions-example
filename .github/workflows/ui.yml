name: UI

on:
  push:
    branches:
      - '**'
    paths:
      - 'ui/**'
      - '*'

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: desktop
    steps:
      - name: Echo
        run: echo "Changed ui"
